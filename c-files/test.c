#include <stdio.h>
#include <string.h>
int main()
{
    char* name[35];
   printf("\nHello, World!. I am learning c, it's so cool ever.");
   printf("Please enter the string:");
   scanf("%s", &name);
   printf("The name is: %s", name);
   return 0; 
}
